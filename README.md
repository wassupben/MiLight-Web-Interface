# MiLight-Web-Interface
A web interface written in node.js for the MiLight wifi bulbs

Clone the repo
``` git clone https://github.com/wassupben/MiLight-Web-Interface.git ```

Install the dependenies
``` npm install  ```

Run the app
``` node app.js ```
